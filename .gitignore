venv/
__pycache__/
# vendored webinar assets (contains a real .env key + 12MB CSV) — never track
Webinar_Agentes/
*.py[cod]
*.egg-info/
.env
data/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
# loop working-state file (memory-loop pattern) — local, not a code artifact
PROGRESS.md
