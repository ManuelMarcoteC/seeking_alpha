"""qtdata — quantitative data pipeline.

Layers: raw (immutable vendor payloads) -> curated (validated canonical tables)
-> DuckDB views (research access, adjusted-on-read).
"""

__version__ = "0.1.0"
